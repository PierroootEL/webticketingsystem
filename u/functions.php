<?php

    // Function

    session_start();

    require '../database/connect.php';

    function log_sent_login(){

        global $sql;
        global $stmt;
        global $pdo;
        global $user;
        global $_SESSION;
        global $_SERVER;


    }

    function log_sent_disconnect(){

    }

?>